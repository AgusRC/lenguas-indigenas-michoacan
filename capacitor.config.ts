import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lenguas-indigenas-michoacan',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
